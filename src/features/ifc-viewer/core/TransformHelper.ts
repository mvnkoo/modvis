import * as THREE from "three";

export class TransformHelper {
  getHelper(geometries: THREE.BufferGeometry[]) {
    const baseHelper = new THREE.Object3D();
    // ... Rest des TransformHelper Codes ...
    return baseHelper;
  }
} 